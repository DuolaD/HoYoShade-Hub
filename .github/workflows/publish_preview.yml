name: Publish Preview (DEPRECATED - DO NOT USE)

# This workflow is deprecated and disabled
# Use publish_preview_2.yml instead
on:
  workflow_dispatch:  # Only allow manual trigger, not automatic on tag push

permissions:
  pull-requests: write
  contents: write


jobs:

  deprecated-notice:
    runs-on: windows-latest
    steps:
    - name: Deprecated Notice
      run: |
        Write-Host "⚠️ This workflow is DEPRECATED!" -ForegroundColor Red
        Write-Host "Please use 'publish_preview_2.yml' instead." -ForegroundColor Yellow
        Write-Host "This workflow uses outdated metadata.ps1 script that is incompatible with the new update system." -ForegroundColor Yellow
        exit 1

